@model IEnumerable<VacationPlanningAssistant.Models.TransportListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TransportId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Type)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Departure)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ReservationNumber)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.TransportId)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Type)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Departure)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.ReservationNumber)
    </td>
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.TransportId }) |
        @Html.ActionLink("Details", "Details", new { id = item.TransportId }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.TransportId })
    </td>
</tr>
}

</table>
